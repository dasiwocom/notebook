# 对标 NotebookLM 调研报告

> 目的：对照 Google NotebookLM（2026 年已更名 *Gemini Notebook*）的实际做法，盘点本项目"多做/少做"了什么，避免自己瞎设计、乱加功能。
> 结论先行：**上传异步化、source 级"处理中"状态、纯来源接地回答、句级引用跳转**是 NotebookLM 的核心范式；我们在后两项上已持平或超出，前两项有收敛空间。

---

## 0. 本项目里到底在跑什么"模型"（先消除疑虑）

最近绕不开的"模型"只有三个，全部都是为了支撑"文档检索问答"，不是新配置的大模型，也都不需要你操作：

| 组件 | 是什么 | 部署 | 用途 | 状态 |
|---|---|---|---|---|
| **DeepSeek (deepseek-chat)** | 云端对话大模型 | api.deepseek.com API | 根据检索到的原文，用中文写回答、生成 [source:N] 引用 | ✅ 一直正常（0.7s 响应） |
| **bge-small-zh-v1.5**（fastembed） | 本地向量嵌入小模型（约 25MB，512 维） | 本机 CPU | 把文档切成的小块转成向量，问答时按语义找最相关段落 | ⚠️ **之前一直在静默失败**（模型从未成功下载，直连 HuggingFace 不通→存的全是空向量）→ 已改用 hf-mirror 镜像下载，全书 438 块已重新嵌入成功 |
| **RapidOCR** | 本地 OCR 引擎 | 本机 CPU | 把"扫描版 PDF（纯图片、没有文字层）"识别成中文文字 | ✅ 正常，335 页缓存都在 |

- 没有引入任何新训练/上网的模型；DeepSeek 是唯一发生云请求的（回答时），bge/OCR 都在本机。
- 之前聊天返回空、卡 135 秒，根因就是 bge 嵌入失败（`Network is unreachable` 连 HF → 空向量 → 无内容），**不是 DeepSeek 或对话链路坏了**。这条已修复并且刚验证：全书 438 块真实向量已重建（`warning: null`）。

---

## 1. NotebookLM 的真实行为（资料核对）

### 1.1 上传与处理（关键：一切异步）
- 上传接口**收到文件立即 200 返回**（内部提示 `Enqueued blob bytes to ... queue`），文件进入异步处理队列（Spanner）。"200"只代表"Google 收下了"，不代表处理完。
- 后台流水线：格式检测/解析（PDF 有文字层→直接解析；扫描件→走 OCR；音频→转录）→ 分块 → 嵌入 → 向量索引 → **source 状态从 "Processing" 变成 "Ready"**。
- 用户界面看到的只有两种状态：处理中 / 已就绪。**没有 OCR 按钮、没有进度条、没有"重新索引"这类实现细节暴露给用户。**
- 处理完成前该 source 不可问，处理期间的请求不受影响（可继续问别的）。

### 1.2 扫描版 PDF
- 官方文档声称支持："PDF ... including scanned documents using optical character recognition (OCR)"。
- 社区实际反馈：**经常导入为空**（`source is empty` / 问啥答"I don't have information"），因为 NotebookLM 读的是 PDF 的文字层，扫描件没文字层就挂，用户得自己先用工具 OCR 再传。
- 结论：**这一点我们实际上已经做得比 NotebookLM 稳**（我们自动检测无文字层→自动 OCR→可检索）。

### 1.3 文档索引策略（大上下文 vs RAG）
- 中小型 source：**整篇直接灌进 Gemini 上下文**（2M token 窗口），不分块、不做检索，保住结构完整。
- 超大 source（超出窗口）：才走 chunk + 嵌入 + 向量检索（Vertex AI Vector Search）。
- 底层：Source Grounding / Discovery Engine（同一套基建支撑 Vertex AI Search）；模型为 Gemini 系列（1.5 Pro → 2.5 → 3.5，用户看不到也不选模型）。

### 1.4 聊天与引用（核心范式）
- 回答**只基于所选 sources**，检索→喂给模型→模型按"每句话标引用"的指令作答。
- 引用是**行内编号 citation**（`[1]` 这种），每个引用可点击 → **跳到 source 原文的准确句子并高亮**，支持跳到图片。回答顶部还有"使用了哪些来源"的信息。
- 引用可随回答一起存为 Note 长期保留。
- 其他：Notebook/Source 面板选择控制"本次回答只看哪些来源"；另有 Studio（学习指南、简报、播客、思维导图等）——不是必需品。

---

## 2. 对照表：NotebookLM vs 我们

| 环节 | NotebookLM | 我们 | 结论 |
|---|---|---|---|
| 上传返回 | 立即 200，异步处理 | 有文字层 PDF：同步解析+嵌入（大文件可能要几十秒）| 🔴 需要收敛 |
| 扫描 PDF | "支持 OCR"但实战常空/要用户自 OCR | 自动检测→后台 OCR→分页缓存→自动可检索 | 🟢 已超出 |
| 处理期间体验 | source 卡显示"Processing→Ready"，不暴露细节 | 我们暴露"提取文字"按钮 + `OCR 12/335` 进度 + 状态轮询 | 🔴 应藏起来 |
| 索引策略 | 小文档整篇直灌；大文档 chunk+向量检索 | 只有 chunk+检索（本地 bge 512 维，SQLite+余弦） | 🟡 可接受（DeepSeek 上下文有限，不必模仿直灌） |
| 回答接地 | 仅来源内容，检索召回后作答 | 同：检索 top-k → DeepSeek 只依据来源作答 + 强制 [source:N] | 🟢 一致 |
| 引用形式 | 行内编号，点击跳到原文句子并高亮 | [source:N] 行内 → 打开 PDF 对应页 + **句子级琥珀高亮框** + OCR 行框回退 | 🟢 持平/更细 |
| 选择问答范围 | Source 面板勾选"只看这些来源" | 库内按文档筛选 | 🟡 有 doc_id 过滤，UI 还够不到面板级 |
| 防幻觉 | 指令+句子级引用可校验 | SUGGESTIONS? 同一套指令+可跳转原文 | 🟢 一致 |

---

## 3. 差距清单（要做才做，等你拍板）

### 3.1 已完成（上传与处理体验已对齐）
1. **上传异步化**：所有文档（md/pdf）上传统一"收到即返回"（实测 ~20ms）→ 后台处理（解析/OCR/嵌入）→ 文档卡显示"处理中"→ 就绪自动可问。已删除前端"提取文字"按钮、进度条、状态轮询等实现痕迹。
2. **后台处理状态**：文档列表出现"处理中"徽标（amber 旋转图标）；失败显示"处理失败·重试"。后台任务用全局锁串行化，保护机器负载。
3. 问答只基于来源 + 句级引用跳转高亮的部分本就达标，未改动。
- 验证：md 与 pdf 上传均 ~20ms 返回、约 3s 后台完成；聊天 1.6s 返回带真实段落引用；全书（438 块）向量已重建为真实向量。

### 3.2 暂不做（与 NB 差异但不值得抄，或受限）
- **小文档整篇直灌**：依赖 2M token 窗口，DeepSeek 上下文有限，语义分块检索更稳。
- **多 Notebook / Note 持久化 / Noteboard**：从"本地工具"定位看是重造轮子。
- **Audio Overview / 学习指南 / 思维导图**：超范围。
- **换 Gemini 模型**：DeepSeek 已是本地无感 API，够用。
- **向量库升级（pgvector/FAISS 等）**：当前容量 438 块×512 维 SQLite 余弦绰绰有余，等真的上几千块再说。

### 3.3 我做技术决策时唯一的硬约束参考
- 对齐 NB 的三条铁律：① 上传即返回、处理后台化；② 用户只见"状态"，不见"流程"；③ 一切回答可溯源到原文句子。
- 之后我再动这类体验相关代码前，先给你一句话说明"要对齐 NB 的哪一条"，得到确认再改。

---

## 4. 当前仓库状态（刚验证完）

- 后端 8000 / 前端 3000 都在跑；前端已加载含"扫描上传自动 OCR"的最新构建。
- 书 `4a48e969`：335 页 OCR 完成、**438 块真实向量已重建**、可检索（待你抽查回答）。
- 库内无垃圾文档；交接备忘在第 4 节同步更新。