fastapi==0.141.1
uvicorn[standard]==0.52.4
openai==3.13.0
numpy==2.5.3
python-dotenv==1.2.3
python-multipart==0.0.32
fastembed==0.8.0
onnxruntime==1.30.0
pymupdf==1.28.2
rapidocr-onnxruntime==1.2.3

# Optional: semantic rerank (bge-reranker). If not installed, falls back to keyword/MMR.
# To enable, run `pip install infinity-emb` first and pin the actual version.
# infinity-emb
