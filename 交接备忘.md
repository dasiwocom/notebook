# notebook 交接备忘

> 更新时间：2026-09-13（深夜收工版）。本地 NotebookLM 复刻项目，RAG 问答（Markdown + PDF + 扫描书 OCR）+ Studio 工具真实产出。
> 重要：不要把本文里的路径/风格约定擅自改掉，改之前先看「设计约束」一节。

---

## 1. 一键重启（每次都这样拉起来）

项目根：`/home/debian/Everything/Projects/notebook`（原 `dasiwo/notebook` 已迁出；`dasiwo` 目录及 brainpress/sync 已废弃删除）。

后端（8000）：
```bash
cd /home/debian/Everything/Projects/notebook/backend
pid=$(ss -ltnp 2>/dev/null | rg ':8000' | rg -o 'pid=[0-9]+' | rg -o '[0-9]+' | head -1)
[ -n "$pid" ] && kill "$pid" 2>/dev/null; sleep 1.5
nohup .venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 > /tmp/opencode/uvi.log 2>&1 &
```

前端（3000）——**standalone 部署**（`output:"standalone"`），必须本地二进制 node 跑 `.next/standalone/server.js`，**禁止 `next start`/`npx next start`**：
```bash
cd /home/debian/Everything/Projects/notebook/frontend
npm run build
cp -r .next/static .next/standalone/.next/static
mkdir -p .next/standalone/public && cp -r public/* .next/standalone/public/
pid=$(ss -ltnp 2>/dev/null | rg ':3000' | rg -o 'pid=[0-9]+' | rg -o '[0-9]+' | head -1)
[ -n "$pid" ] && kill "$pid" 2>/dev/null; sleep 1
cd .next/standalone
setsid env HOSTNAME=127.0.0.1 PORT=3000 node server.js > /tmp/notebook-front.log 2>&1 < /dev/null &
```
> 每次改前端代码后都要：`build` → 拷 static/public → 重启。生产模式才有完整效果，不要用 dev 模式。

健康检查：`curl -s http://127.0.0.1:8000/api/health` → `{"status":"ok"}`；`curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3000` → 200；favicon `http://127.0.0.1:3000/favicon.svg` → 200。
> setsid 后台启动后 shell 可能报 "timeout"——属正常现象，用 curl 验证即可。

## 2. 架构 / 端口 / 目录

| 项 | 值 |
|---|---|
| 前端 | Next.js 16.2.7（standalone），`frontend/`；运行 `node .next/standalone/server.js` |
| 后端 | FastAPI（uvicorn），`backend/`，venv `backend/.venv` |
| 聊天模型 | DeepSeek（`backend/.env` 的 `DEEPSEEK_API_KEY`） |
| 嵌入/重排 | 本地 `BAAI/bge-small-zh-v1.5`（fastembed）、`BAAI/bge-reranker-base` |
| OCR | `rapidocr_onnxruntime`，扫描件自动后台识别 |
| 数据库 | SQLite `backend/data/app.db` |
| PDF/data | `backend/data/pdf/{doc_id}.pdf`、`pdfpages/{doc_id}/{n}.png`（110 DPI）、`pdf_ocr/{doc_id}/{n}.json`（150 DPI） |

后端关键文件：`app/main.py`（路由）、`app/pdf.py`、`app/ocr.py`、`app/chunker.py`、`app/retriever.py`（MMR）、`app/chat.py`（NDJSON 流+引用+意图路由）、`app/study.py`（学习模式）、`app/judge.py`（意图分类）、`app/settings.py`（运行时设置热更新）、`app/db.py`、`app/config.py`；`data/tocs.json`（书目录→起始页映射）。
前端关键文件：`app/page.tsx`（三栏 + Sources/Chat/Studio 移动端页签、conversationIds）、`components/DocumentPanel.tsx`（左：文档列表/预览/勾选）、`components/ToolsPanel.tsx`（右：Studio 5 个工具 + 产出卡片区）、`components/ChatPanel.tsx`（中：对话流）、`components/SettingsPanel.tsx`、`components/ThemeToggle.tsx`、`globals.css`（设计变量/toolbox）、`lib/api.ts`（`chatStream` 复用）、`public/favicon.svg`（🤓）。

## 3. 已知坑（重要）

1. **前端是 standalone**：启动/重启只能 `node .next/standalone/server.js`（见第 1 节），不要 `next start`/`npx next start`；有人曾因此误装到别的 next 版本导致连不上。
2. **pip 装机**：默认源超时，必须清华源 `pip install <包> -i https://pypi.tuna.tsinghua.edu.cn/simple --timeout 30`。
3. **上传异步对齐 NotebookLM**：收文件即返回（<20ms），后台统一「解析→OCR(扫描件)→分块→嵌入→ready」；列表只显示"处理中→就绪"，无 OCR 按钮/进度轮询；失败才在面板给"重试"。
4. **扫描版 PDF 无文字层**自动用 RapidOCR，用户无感。
5. **OCR 每本新书只跑一次**，逐页缓存 `data/pdf_ocr/{doc_id}/{n}.json`，重索引复用、断点续跑；`OCR_WORKERS=2`、`OCR_DPI=110`、`OCR_PARTIAL_EVERY=40`（先头几章可检索）。全书一次约 15–25 分钟。
6. **OCR 产物页级用 `# 第 N 页`、章 `##`、节 `###`**（前端跳页正则会取"第 N 页"），别改级别。
7. **高亮定位分两种**：文字层 PDF 用词框，扫描页回退用 OCR 行框（有缓存才走）。
8. **分句正则必须含 ASCII `.`**。
9. **UI 配色已全部按用户最新指示重选**（见「设计约束」），别换回旧灰/其他蓝。
10. 库中文档是用户资料，**不要删**；测试 PDF 用完要删（`DELETE /api/documents/{id}`）。
11. `/tmp/opencode/` 放调试脚本/日志/截图；验证以数值/curl/控制台为准。
12. **对话是"有判断力的学习助手"**（对标 NotebookLM）：请求先过 `judge.classify()`（qa/explain/study_plan/key_points/self_test/meta/clarify）；空检索不再硬拒，会补讲/引导/澄清。
13. **学习模式 `study.py`**：基于 `data/tocs.json` 目录＋各章起始页生成带 `[source:N]` 的速通/重点/自测；新书目录要先进 `tocs.json`，否则回退正文 `## 第X章` 解析可能被 OCR 噪声污染。
14. **空块过滤 + 章节限定检索**：OCR 空白/页脚碎片不落库（`_is_blank_chunk`）；`retriever.search(page_range=…)` 按"第 N 章/节"限定页码。
15. **空块不落库**：否则 `chunk_count≠indexed_chunks` 前端会一直琥珀点。
16. **讲解要具体有脑子**：`EXPLAIN_PROMPT` 反套模板、点名章题/页范围、逐句 `[source:N]`；`judge.classify` 判 clarify 但点明章节/概念时强制转 explain。
17. **前端引用渲染**：`[source:N]` 先经 `ChatPanel.toCiteMarkdown` 转 `[N+1](#cite-N)` 角标；"引用来源"列表默认折叠。
18. **引用角标去噪**：连续同源只亮一次、同源最多 2 次、每条 ≤48，多余吞掉；来源列表按 chunk_id 去重。
19. **PDF 跳转页号解析**：citation.section 是 `第 N 页 > 标题…`，页号在首段，用 `match(/第\s*(\d+)\s*页/)`（不要 `.split(">").pop()`）。
20. **停止生成 + 复制**：流式时按钮变红色停止；复制内容 `[source:N]`→`[N]`。
21. **检索 MMR**：`retriever.py` 重排后 MMR（`MMR_LAMBDA=0.7`）去冗余、引用分散到不同页。
22. **设置面板**：对话/嵌入/检索参数界面热更新不用重启；API key 永不回传前端（只给 `*_SET`）；存 `settings.json`，`.env` 为默认基线。端点 `GET/PUT /api/settings`、`POST /api/settings/test`。
23. **Electron 桌面版**：`electron/` 外壳，动态端口 + 拉 FastAPI + Next standalone；Linux AppImage 自包含，Win/mac 首启装依赖；桌面数据落 `userData`（`~/.config/notebook/`）；构建 `npm run dist`（Linux）、`dist:win`（Win/wine）、`dist:mac`（macOS）。**注意：`electron/dist` 里还是旧品牌，尚未重新打包。**

## 4. 当前状态（今晚为止）

- 两个服务都跑着（8000/3000），重启方式见第 1 节。
- 库中现有：《嵌入式系统设计基础及应用》`4a48e969b2744a4d`（已 OCR：335 页、30.4 万字符、351 块全部带向量）。
- **三栏已定型为 Sources（左）/ Chat（中）/ Studio（右）**，移动端顶部栏下有 Sources/Chat/Studio 页签（sky-400 下划线高亮）。
- **Studio 工具真实产出（核心新功能）**：勾选文档（左列 checkbox=加入对话，`conversationIds`，持久化到 localStorage `bs:conv`）后，点工具即在 Studio 底部产出区直接流式生成卡片——**不再往对话发消息**。5 个工具：Fast Track / Key Points / Self Test / Chapters / **Flashcards**（新增）。复用 `chatStream`（`/api/chat`，`doc_id:null` + `doc_ids:conversationIds`），工具 prompt 自带意图（分级 study 模式）。产出卡片支持终止（Stop）、删除（X）、Markdown 排版渲染（`prose`）、`[source:N]`→`[N]` 编号。空态提示语已更新。
- **Markdown 渲染排版已补**：对话回答和 Studio 产出都套了 `prose` typography（标题/段落/列表/表格/引用块有层次），不再"顶格一行话"。后端实测流出的是完整 Markdown（表/引用/代码都在）。
- **左面板**：预览顶部栏与列表一致显示 "Sources"+数量，缩小图标在预览时=返回列表（`onBack`）、列表时=折叠面板；行点击进预览，checkbox 勾选=加入对话（工具可用性以此判断 `conversationIds.size>0`）。
- **AI 回答质量**：实测导出示例（NVIC 讲解 / 全书速通）都是完整中文 Markdown，检索+章节目录路数正确。
- 已实现（承接此前）：MD/PDF 上传、三栏拖拽/双击复位、拖动上传、PDF 页跳转+琥珀闪框+引用高亮、扫描书自动 OCR、深色/亮色、学习模式、设置面板、判断力对话。

## 5. 设计约束（改 UI 前先读）

- **配色（用户敲定，勿回退）**：亮 `--background:#edeffa / --card:#ffffff / --shadow-btn:#ebebeb / --shadow-soft:#f9f9f9 / --shadow-strong:#ebebeb`；暗 `--background:#1a1d22 / --card:#22262b / --shadow-btn:#33373b / --shadow-soft:#22262b / --shadow-strong:#1f2327`。黑色胶囊按钮亮 `#000000`/暗 `#ffffff`。
- **发送/停止键**：空输入默认亮 `#ebebeb`/暗 `#33373b`（右箭头）；有输入 `#4259ff`；思考中 `#db372d`（方块=停止）。
- **面板底色**：对话卡片区用 `--card`；Sources 展开面板= `#ffffff/#22262b`；用户气泡= `#edeffa/#1a1d22`；文件行 hover= `#edeffa/#1a1d22`，选中行暗 `#32343e`。
- **工具框 `.toolbox`**：选中文档=同亮暗一致的彩色浅底（**暗色不做叠加淡染**，用 `.toolbox` 统一 `var(--tool-bg)`）；未选中 `.toolbox-muted` 亮 `#ebebeb`/暗 `#1f2327`、工具文字保持深色。彩色序列：`#edeffa #f2f2e8 #e1f1e5 #f0e9ef #f7edeb …`（Fast Track/Key Points/Self Test/Chapters/Flashcards）。
- **面板头**：只显示标题（Sources/Chat/Studio，`text-[15px]`，无左侧图标）；右侧控制——Sources=`Minimize2`（对角缩小，预览时=返回列表）、Chat=三点菜单（Clear chat / Settings）、Studio=`PanelRight`。图标按钮 `p-2`、`h-4`（用户嫌小，已统一放大）。
- **Sources 工具栏**：排序下拉（Recent/Name/Characters）在左；"Select all"在右、**文字在复选框左边**，复选框方形无圆角 `border-2` 加粗，右缘与行勾选框对齐。
- **UI 文案统一英文**（用户确认过，先"统一中文"后改回英文）：`lang="en"`、英文 meta、按钮/提示全英文；后端 prompt/代码注释可中文。品牌 "notebook" + 🤓（header 30px + favicon.svg）。
- **移动端**：页签在顶部栏下（不是底部），用 `data-mb`/`mb-view mb-doc mb-tools` 控制显示/隐藏；左/右面板在移动端 `md:flex` 启用。
- 主题只有亮/暗两档；日夜过渡统一 `*,*::before,*::after` `transition .25s`，SVG 图标 `backface-visibility:hidden` + `button:has(svg){transform:translateZ(0)}`。
- 未选中文档（无 conversationIds）时：Studio 工具灰但工具可点无意义，应保持"先勾文档再点工具"。

## 6. 常用接口（速查）

- 上传：`POST /api/documents`（multipart file，.md/.pdf，异步秒回）
- 列表/详情：`GET /api/documents`、`GET /api/documents/{id}`
- 页数/页图/高亮：`GET .../pages`、`/pages/{n}.png`、`/pages/{n}/highlights?q=&snippet=`
- 重索引：`POST /api/documents/{id}/reindex`、进度 `GET .../reindex`
- 删除：`DELETE /api/documents/{id}`；改名：`PATCH /api/documents/{id}`
- 对话：`POST /api/chat`（NDJSON 流 `delta`/`done`/`suggest`/`error`；body `message + history + doc_id(null) + doc_ids(conversationIds)`)
- 设置：`GET/PUT /api/settings`、`POST /api/settings/test`

## 7. 后续候选方向（未做）

- **Ollama 本地模型切换**（`backend/.env` 换 base_url，用户提过但未确认）。
- **electron/dist 里旧品牌未重新打包**（改品牌后 dist 还没跑过 `npm run dist`）。
- **工具产出卡未持久化**（刷新即失，考虑存 localStorage）；mind map/quiz/flashcard 可考虑解析成卡片/脑图组件而非纯 Markdown。
- 多会话历史、内容导出（Markdown/PDF）、页眉页脚去噪、通用目录自动识别入库（目前仅《嵌入式系统》预录 `tocs.json`）。